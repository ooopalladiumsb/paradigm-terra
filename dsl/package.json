{
  "name": "@paradigm-terra/dsl",
  "version": "0.1.0",
  "description": "Reference implementation of DSL v1.2 (Constraint DSL v1.1 + CAL v0.1.0-draft extensions): parser, validator, total evaluator, cost model, and DSL_HASH.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "src",
    "vectors",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "vectors:generate": "node --import tsx scripts/generate-vectors.ts",
    "vectors:verify": "node --import tsx --test test/golden-vectors.test.ts"
  },
  "dependencies": {
    "@paradigm-terra/canonical": "file:../canonical"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
